# Minimal QiskitOperator Validation Service Dependencies
# Focused on circuit validation only (no IBM runtime)
# Target size: ~300-400MB

# FastAPI and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# Core Qiskit only (no IBM runtime - saves ~200MB)
qiskit==1.0.0

# Utilities
python-multipart==0.0.6

