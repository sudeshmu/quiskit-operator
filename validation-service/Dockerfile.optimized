# QiskitOperator Validation Service - Optimized Dockerfile
# Target size: ~400-500MB (down from 1.34GB)
#
# Multi-stage build to separate build dependencies from runtime

# ============================================
# Stage 1: Builder - Install dependencies
# ============================================
FROM python:3.11-slim AS builder

WORKDIR /build

# Install build dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    g++ \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements and build wheels
COPY requirements.txt .

# Build wheels for all dependencies (enables better caching and smaller final image)
RUN pip wheel --no-cache-dir --wheel-dir /wheels -r requirements.txt

# ============================================
# Stage 2: Runtime - Minimal final image
# ============================================
FROM python:3.11-slim

WORKDIR /app

# Install only runtime dependencies (no gcc/g++)
RUN apt-get update && apt-get install -y --no-install-recommends \
    # Required for numpy/scipy at runtime
    libgomp1 \
    && rm -rf /var/lib/apt/lists/* \
    && apt-get clean

# Copy pre-built wheels from builder stage
COPY --from=builder /wheels /wheels
COPY --from=builder /build/requirements.txt .

# Install from wheels (much faster, no compilation needed)
RUN pip install --no-cache-dir --no-index --find-links=/wheels -r requirements.txt \
    && rm -rf /wheels

# Copy application code
COPY main.py .

# Create non-root user for security
RUN useradd -m -u 1000 qiskit && chown -R qiskit:qiskit /app
USER qiskit

# Expose port
EXPOSE 8000

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"

# Run the application
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]

